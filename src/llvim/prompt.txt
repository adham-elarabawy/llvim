Return a json list of commands to be executed in sequence, such that the final state clipboard contains the content that the extraction query is requesting. 

### Extraction Query (this isn't the verbatim text you have to extract, just a rough request)
{extraction_instructions}

### Document Text
{document_text}